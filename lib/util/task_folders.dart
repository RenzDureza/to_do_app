import 'package:flutter/material.dart';

class taskFolders extends StatelessWidget {
  const taskFolders({super.key});

  @override
  Widget build(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.all(8),
      child: Column(
        children: [
          
        ],
      ),
    );
  }
}