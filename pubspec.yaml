name: to_do_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_slidable: ^3.1.1
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

  hive_generator: ^2.0.1
  build_runner: ^2.1.11
  flutter_launcher_icons: ^0.13.1
flutter_icons:
  image_path: "icons/list_8476676.png"
  android: true
  ios: true

flutter:
  uses-material-design: true
